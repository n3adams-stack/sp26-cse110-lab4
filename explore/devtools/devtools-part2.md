### Part 2 Questions
1. The bug was that both nums are strings, so they are being concatenaed instead of being treated like numbers and add it
2. I would add an explicit type conversion to make sure both inputs are nums